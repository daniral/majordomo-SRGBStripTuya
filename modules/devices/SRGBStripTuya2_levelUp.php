<?php
/**
 * Увеличивает яркость лампы.
 *      callMethod('имя объекта.levelUp', array("value"=>1--100));
 * Если передан массив с ключом `value`, яркость увеличивается на указанное значение (1–100).
 * Если параметр не передан, используется значение по умолчанию: 10.
 *
 * @param array{
 *     value?: int|null   // На сколько увеличить яркость (1–100). По умолчанию 10.
 * } $params Ассоциативный массив параметров.
 *
 * @return void
 */

adjustProperty($this, 'level', $params['value'] ?? null, 'up');